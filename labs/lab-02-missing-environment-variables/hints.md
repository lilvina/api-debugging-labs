# Hints

## Hint 1

Look closely at the order of operations in `app.py`.

## Hint 2

When does the code read `WEATHER_API_KEY`?

## Hint 3

The `.env` file must be loaded before calling `os.getenv()`.
