# Hints

## Hint 1

Print or inspect the value of `auth_header`. What exact string is being received?

## Hint 2

Compare what the server expects with what the client sends. Are they formatted the same way?

# Hint 3

The client sends the key with a `Bearer` prefix. The server needs to account for that format.